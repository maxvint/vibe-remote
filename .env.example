# IM Platform Selection
# Options: slack,telegram
IM_PLATFORM=slack

# ============== Slack Configuration ==============
# Required for Slack
SLACK_BOT_TOKEN=xoxb-your-slack-bot-token

# Required for Socket Mode (recommended)
SLACK_APP_TOKEN=xapp-your-slack-app-token

# Optional: For webhook mode instead of Socket Mode
SLACK_SIGNING_SECRET=your-signing-secret

# Optional: Whitelist of allowed channel IDs (comma-separated)
# Examples:
#   null or omit: Accept all channels
#   [C1234567890,C0987654321]: Only accept messages from these channel IDs
SLACK_TARGET_CHANNEL=[]

# Optional: Require @mention in channels (ignored in DMs)
# If true, bot only responds when @mentioned in channels. Default: false
SLACK_REQUIRE_MENTION=false

# ============== Telegram Configuration ==============
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here

# Optional: Whitelist of allowed chat IDs (comma-separated)
# Examples:
#   null or omit: Accept all chats
#   [123456789,987654321]: Only accept messages from these chat IDs
TELEGRAM_TARGET_CHAT_ID=[]

# Claude Configuration
CLAUDE_PERMISSION_MODE=bypassPermissions
CLAUDE_DEFAULT_CWD=./_tmp
# Optional: Custom system prompt
CLAUDE_SYSTEM_PROMPT=

# Application Configuration
LOG_LEVEL=INFO

# Agent routing & Codex (optional)
# Enable Codex CLI support (requires codex to be installed locally). Defaults to true.
CODEX_ENABLED=true
# Override Codex CLI binary (defaults to "codex")
CODEX_CLI_PATH=codex
# Extra CLI args appended to every codex exec invocation (use shell-style quoting)
CODEX_EXTRA_ARGS=
# Optionally force a specific Codex model
CODEX_DEFAULT_MODEL=

# OpenCode (optional)
# Enable OpenCode support (requires opencode to be installed). Defaults to false.
OPENCODE_ENABLED=false
# Override OpenCode CLI binary (defaults to "opencode")
OPENCODE_CLI_PATH=opencode
# OpenCode server port (defaults to 4096)
OPENCODE_PORT=4096
# OpenCode API request timeout in seconds (defaults to 30). Set 0 to disable.
OPENCODE_REQUEST_TIMEOUT=30
# Note: OpenCode agent/model/reasoningEffort settings are read from ~/.config/opencode/opencode.json
# and can be overridden per-channel via the Agent Settings dialog in Slack.

# Path to agent routing file (YAML/JSON). Defaults to config/agent_routes.yaml when present.
AGENT_ROUTE_FILE=
